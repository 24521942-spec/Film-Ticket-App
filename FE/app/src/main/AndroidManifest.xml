<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicketBookingApp">

        <!-- HomeActivity (màn hình chính sau Splash) -->
        <activity
            android:name="com.example.ticketbookingapp.screens.HomeActivity"
            android:exported="false" />

        <!-- Splash (màn đầu tiên) -->
        <activity
            android:name="com.example.ticketbookingapp.screens.SplashActivity"
        <activity
            android:name=".screens.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

        <activity
            android:name=".screens.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".screens.MainActivity"
            android:exported="false" />

    </application>
</manifest>
